# 阶段 C：章节分割

> 摘自 guide/guide.md 第 708-726 行。本文件是该阶段的完整执行指令。

## 需要的变量

无额外变量。

## 前置检查

执行本阶段前，读取 `COURSE_DIR/pipeline-progress.md`：
- 确认阶段 C 状态为"待执行"
- 如果状态为"已完成"，跳过本阶段，直接进入阶段转换
- 如果状态不是"待执行"也不是"已完成"，报错并停止

## 执行指令

按一级标题拆分为独立 Markdown 文件：

- **命名规范**：`XX-章节标题.md`，其中 `XX` 为两位数序号（`00`、`01`、`02`...）
- **标题来源**：直接从 Markdown 中读取的一级标题原文提取，不硬编码。有的教材有"序""前言""符号表"，有的直接从"第1章"开始，按实际结构来
- **文件位置**：所有文件放入 `textbook/chapters/`（包括总 Markdown 和分割后的各章文件）
- **总 Markdown**：原样保留在 `textbook/chapters/` 中不修改，分割出的各章是独立副本

示例（假设教材有"序"和 15 章）：
```text
textbook/chapters/
├── 总教材.md              ← 原始总文件，不修改
├── 00-序.md
├── 01-第1章-绪论.md
├── 02-第2章-机器学习概述.md
├── ...
└── 15-第15章-序列生成模型.md
```

## 质量门

- `textbook/chapters/` 目录存在
- 文件名匹配 `XX-*.md` 格式
- 原始总文件保留在 `textbook/chapters/` 中
- 分割出的章节数量与阶段 B 识别的一致

## 完成记录

质量门通过后，更新 `COURSE_DIR/pipeline-progress.md`：
- 将阶段 C 状态从"待执行"改为"已完成"
- 记录完成时间
- 记录生成的章节文件列表

## 阶段转换

完成记录写入后：
1. 读取 `COURSE_DIR/pipeline-progress.md`
2. 找到第一个状态为"待执行"的阶段
3. 如果找到 → 读取对应的 `guide/pipeline/stage-{x}.md`，执行该阶段
4. 如果没有"待执行"阶段 → 所有阶段完成，进入清理协议
