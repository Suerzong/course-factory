# 第9章 单元练习规格

本文件约束第9章每个教学单元可以生成什么类型的题。实际题目可以动态生成，但必须同时满足 `course-rules.md`、`knowledge/coverage/chapter-09-coverage.md`、对应教学指引和本文件。

## 使用规则

- 正式题只允许来自当前单元或已解锁前置单元的 `出题权限=正式题` 内容。
- `课堂识别不计分` 只能作为讲解中 1 句轻量确认，不写入正确率。
- `了解即可`、参考文献、工具 URL、人物年份细节、后文章节算法细节不进入正式题。
- 每道题必须标注 `对应单元`、`教材段落`、`知识点层级` 和 `未学内容检查`。
- 若题目依赖未解锁单元，必须先降级、改写或推迟。

## 单元规格表

| 单元ID | 教材段落 | 允许正式题型 | 正式题来源边界 | 未学内容拦截 |
|---|---|---|---|---|
| `ch09-00` | `¶0001-¶0011` | 概念复述、分类归纳、关系解释 | 无监督学习定义；三类无监督学习问题；无监督特征学习用途；参数与非参数密度估计区别；无监督学习三要素 | 不考具体算法公式，不考 Hinton 引用的数字细节，不考 K-Means 算法 |
| `ch09-01` | `¶0012-¶0022` | 概念复述、公式角色解释、边界判断 | PCA 定义与目标；投影向量约束 w^T w=1；投影表示 z^(n)=w^T x^(n)；投影方差公式含义；协方差矩阵 Σ 定义 | 不考特征值分解推导，不考多维投影，不要求解具体数值 |
| `ch09-02` | `¶0023-¶0033` | 推导复述、概念串联、关系解释 | 拉格朗日方法推导 Σw=λw；PCA 与特征值分解关系；λ 即投影方差；多维投影取前 D' 个特征向量；PCA 不保证类别可分性 | 不考 LDA 算法细节，不考特征值数值计算，不展开习题9-3 |
| `ch09-03` | `¶0034-¶0044` | 概念复述、关系解释、边界判断 | 稀疏编码生物学动机；编码定义 x=Az；完备与过完备基向量概念；过完备+稀疏性限制得到唯一编码 | 不考生物视觉皮层细节，不考 Olshausen 实验细节，不考 PCA 编码的稠密性推导 |
| `ch09-04` | `¶0045-¶0059` | 公式角色解释、概念对比、边界判断 | 目标函数 L(A,Z)；η 超参数角色；ℓ₀ 范数定义及不可导问题；ℓ₁ 范数替代方案；对数函数和指数函数形式 | 不考 ℓ₀ 优化算法细节，不考对数/指数函数的梯度推导，不展开习题9-6 |
| `ch09-05` | `¶0060-¶0070` | 流程复述、概念归纳、原因解释 | 交替优化两步流程；稀疏编码三个优点（计算量、可解释性、特征选择） | 不考优化算法收敛性分析，不考基向量 A 的正则化系数调参 |
| `ch09-06` | `¶0071-¶0079` | 概念复述、结构解释、边界判断 | 自编码器定义；编码器与解码器角色；重构错误最小化目标；M<D 为降维；M≥D 需附加约束 | 不考具体网络层数与激活函数，不考单位函数推导，不考聚类转换细节 |
| `ch09-07` | `¶0080-¶0091` | 结构复述、公式角色解释、术语解释 | 两层网络结构（输入-隐藏-输出）；编码公式 z=f(W^(1)x+b^(1))；解码公式 x'=f(W^(2)z+b^(2))；捆绑权重定义与作用；重构错误损失函数；训练后只保留编码器 | 不考反向传播训练细节，不考权重初始化方法 |
| `ch09-08` | `¶0092-¶0104` | 概念复述、公式角色解释、关系解释 | 稀疏自编码器 M>D 且 z 稀疏的动机；目标函数三部分；ρ̂_j 平均活性值公式；KL 距离衡量稀疏性；ρ(Z) 求和形式 | 不考 KL 距离的梯度推导，不考 ρ* 具体调参策略 |
| `ch09-09` | `¶0105-¶0111` | 概念复述、机制解释、对比题 | 堆叠自编码器逐层训练思想；降噪自编码器引入噪声增加鲁棒性；损坏比例 μ 随机置零机制；从 x̃ 重构 x 的目标 | 不考逐层训练的具体算法细节，不考 Bengio 和 Vincent 论文细节，不考图9.3具体架构 |
| `ch09-10` | `¶0112-¶0119` | 概念复述、公式角色解释、流程解释 | 密度估计定义；参数与非参数分类；对数似然函数公式；MLE 的优化形式 θ^ML=argmax Σ log p(x^(n);θ) | 不考具体分布的推导，不考 MLE 的统计性质（一致性、无偏性） |
| `ch09-11` | `¶0120-¶0127` | 公式复述、推导复述、概念解释 | 正态分布概率密度函数；对数似然函数；μ^ML 和 Σ^ML 的 MLE 解的形式 | 不考偏导数具体推导步骤，不考样本协方差的有偏/无偏修正 |
| `ch09-12` | `¶0128-¶0141` | 公式复述、推导复述、约束优化解释 | 多项分布 one-hot 表示与概率密度函数；约束 Σμ_k=1；拉格朗日乘子法；μ_k^ML=m_k/N | 不考拉格朗日乘子法推导细节，不考附录 C.3 和 D.2.2.1 的内容，不考多项式系数 |
| `ch09-13` | `¶0142-¶0159` | 问题归纳、概念对比、公式解释 | 参数密度估计三个问题；非参数密度估计定义；p(x)≈K/(NV) 核心公式；两种非参数方式（固定V/固定K） | 不考 EM 算法（第11章），不考二项分布推导，不考 P≈K/N 的期望与方差推导 |
| `ch09-14` | `¶0160-¶0167` | 概念复述、公式解释、原因分析 | 直方图方法区间划分与密度公式 p_m=K_m/(NΔ_m)；区间宽度 Δ 对估计的影响；维度灾难 M^D | 不考皮尔逊历史细节，不考图9.4的具体数据，不考直方图与核方法的对比 |
| `ch09-15` | `¶0168-¶0184` | 概念复述、公式角色解释、对比题 | 核密度估计定义（Parzen窗）；超立方体核函数；密度公式 p(x)=K/(NH^D)；高斯核函数；K近邻方法可变宽度思想 | 不考 K 近邻分类器错误率证明（Cover et al.），不考核函数选择理论，不展开习题9-5 |
| `ch09-16` | `¶0185-¶0190` | 章节串联、总结复述、关系解释 | 无监督学习三大问题类型；无监督特征学习作为表示学习；参数与非参数密度估计分类；无监督学习缺少客观评价方法 | 不考后文章节算法（EM、玻尔兹曼机、VAE/GAN），不考参考文献作者与书名 |
| `ch09-test` | 第9章已解锁段落 | 章测综合题、错因纠正、跨节串联 | 只从已通过单元的正式题范围抽取 | 不考未通过单元，不考了解即可和参考文献内容 |

## 章测约束

- 章测前必须确认 `knowledge/coverage/chapter-09-coverage.md` 中前 17 个单元均有掌握证据。
- 章测题至少 70% 来自核心概念、关系和边界，最多 30% 用于跨节串联。
- `ch09-16` 可以轻量纳入章节总结关系，不得考参考文献作者、书名或后文章节算法细节。
- 正式题中不得出现作者名、论文年份、URL 或外部教材引用。
